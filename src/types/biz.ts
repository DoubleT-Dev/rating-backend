import {Category} from '../types/category'

export type Biz = {
    id: string;
    name_en: string;
    name_mm: string;
    categories_id : string;
    is_active : boolean;
    description : string
    // categories: Category
  };
  